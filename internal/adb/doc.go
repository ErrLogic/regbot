// Package adb provides minimal wrappers around the Android Debug Bridge (adb)
// used for pre-flight checks (device presence) and optional APK installation.
// It is never used to drive the UI.
package adb
